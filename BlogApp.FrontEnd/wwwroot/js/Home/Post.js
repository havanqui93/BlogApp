var Post = (function ($container) {
    const URL_POST = '';

    var constMap, stateMap, submit,

        init;

    stateMap = {
        data: {

        }
    };

    init = () => {}

    return {
        init
    }
}($('.create-post')))

Post.init();